name: Build APK
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取仓库源码
        uses: actions/checkout@v4

      - name: 配置JDK17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: temurin

      - name: 授予gradlew执行权限
        run: chmod +x gradlew

      # 新增：自动生成匹配8.4版本完整gradle-wrapper.jar，彻底解决类缺失报错
      - name: 修复Gradle Wrapper 8.4
        run: ./gradlew wrapper --gradle-version 8.4

      - name: 下载OpenCV 4.9 SDK
        run: |
          wget -O opencv-sdk.zip https://github.com/opencv/opencv/releases/download/4.9.0/opencv-4.9.0-android-sdk.zip
          unzip -q opencv-sdk.zip
          cp -r OpenCV-android-sdk/sdk/java ./opencv
          mkdir -p ./opencv/src/main/jniLibs
          cp -r OpenCV-android-sdk/sdk/native/libs/arm64-v8a ./opencv/src/main/jniLibs/

      - name: 查看项目模块任务
        run: ./gradlew tasks

      # 指定app模块编译
      - name: 编译APP Debug安装包
        run: ./gradlew :app:assembleDebug

      - name: 导出成品APK
        uses: actions/upload-artifact@v4
        with:
          name: LingMiao_成品APK
          path: app/build/outputs/apk/debug/app-debug.apk
